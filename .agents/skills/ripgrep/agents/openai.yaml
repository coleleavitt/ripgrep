interface:
  display_name: "Ripgrep"
  short_description: "Repo-specific patterns and workflows for ripgrep"
  default_prompt: "Use the ripgrep repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true